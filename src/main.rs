//! Engine binary entry point.
//!
//! Eventually this runs the UCI loop (Phase 1). For now it just proves the
//! crate builds and links against the library.

fn main() {
    println!("chess-engine-nnue — Phase 0 (board representation). See docs/.");
}
